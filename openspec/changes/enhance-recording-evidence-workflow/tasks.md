## 1. 数据模型与生命周期基础

- [x] 1.1 扩展 `VideoRecord` 和 SQLite 迁移，保存照片本地/网络路径、拍摄时间、宽高、大小、哈希、状态及错误，并保证旧数据库和旧录像可读
- [x] 1.2 增加持久化媒体后处理任务及阶段状态，支持按结束时间倒序查询、取消、重试和重启恢复
- [x] 1.3 增加持久化人工删除意图及分资产完成状态，使网络离线删除可恢复且幂等
- [x] 1.4 将视频、原始照片和联合证明建模为同一录像聚合资产，并统一解析网络优先、本地兜底的可用路径
- [x] 1.5 更新存储用量、保留期限预测和清理候选统计，使其包含照片及仍待处理的源文件

## 2. 放大范围与条码识别

- [x] 2.1 提取唯一的中央放大裁剪计算，供智能放大、识别 ROI 和主界面引导框共同使用，并覆盖放大关闭和边界倍率测试
- [x] 2.2 取消条码解码前的整帧固定尺寸降采样，增加低成本候选定位和原始分辨率候选裁剪
- [x] 2.3 为候选区域增加有上限的灰度、局部对比度、锐化、二值化和多尺度解码快速退出链路
- [x] 2.4 保持最新帧覆盖旧帧的单工作线程模型，并增加 N100 内存分配、慢解码和丢帧回归测试
- [x] 2.5 扩展连续身份确认，记录条码中心、尺度、角度、清晰度和曝光稳定指标，并把候选/稳定/失败原因反馈到主界面
- [ ] 2.6 使用实际快递面单样本核对 CODE 128、CODE 39、ITF 等格式范围，只有经样本确认后才增加额外格式

## 3. 稳定全分辨率抓拍

- [x] 3.1 扩展摄像头能力检测，区分独立静态通道和视频模式，并实际验证每个设备最高像素模式能否在限定时间内取帧
- [x] 3.2 实现分辨率归一化的全画面运动、清晰度和曝光稳定评估，以及短时帧组中的最清晰合格帧选择
- [x] 3.3 实现统一录像启动状态机，使摄像头识别、扫码枪、网页联动、连续换单和手动无单号录制都必须经过独立抓拍
- [x] 3.4 实现独立静态通道优先的最高原生分辨率抓拍，并验证抓拍不改变照片完整视野
- [x] 3.5 实现无静态通道摄像头的临时模式切换、暖机帧丢弃、稳定抓拍、录像模式恢复和有效首帧确认
- [x] 3.6 将照片以高质量 JPEG 写入当前录像的本地工作目录，通过临时文件原子发布并在视频开录前绑定数据库记录
- [x] 3.7 在照片无法稳定获取、摄像头模式无法恢复或文件写入失败时阻止静默开录，并提供可操作的状态和错误提示
- [x] 3.8 为连续换单和并发触发增加照片归属测试，确保每段录像获得独立照片且不会错绑相邻记录

## 4. 实时AAC与可恢复录像容器

- [x] 4.1 设计并实现 FFmpeg 第二输入管道，把现有 WASAPI 规范化 PCM 实时编码为 AAC-LC 并写入同一临时 MKV
- [x] 4.2 保留音频通道选择、重采样、断流补偿、音量和时间线统计，同时移除正常录像路径的 WAV 写入及恢复假设
- [x] 4.3 更新 FFmpeg 启停和同步时钟，验证 AAC 初始化失败、音频中断、视频管道失败和强制结束后的可恢复行为
- [x] 4.4 将最终 MKV 到 MP4 参数改为音视频双流复制，并验证 H.264、H.265、AV1 与可用硬件编码器组合

## 5. 空闲后处理、联合证明与网络归档

- [x] 5.1 实现统一录像活动闸门，把等待稳定、抓拍、摄像头切换、启动、录制和停止均视为高优先级活动
- [x] 5.2 实现连续空闲 60 秒后启动、按 `EndTime DESC` 最新优先、单任务串行和每条完成后暂停 10 秒的调度器
- [x] 5.3 让新录像活动抢占 FFmpeg 封装、媒体校验、哈希、网络复制和清理，并安全恢复被取消任务的待处理状态
- [x] 5.4 使用 `.mp4.writing` 和生命周期锁完成校验及原子发布，增加“任务 A 已发布、任务 B 失败清理”竞争顺序回归测试
- [x] 5.5 将证明格式升级为同时签名最终 MP4 与原始照片的版本化载荷，并保持旧版视频证明可验证
- [x] 5.6 重排归档顺序，确保最终 MP4、照片和联合证明全部完成后才入队，并对三个网络目标分别做长度和哈希验证
- [x] 5.7 更新本地两日副本清理和长期保留逻辑，使任一资产处于封装、签名、复制、删除或未知状态时禁止清理
- [x] 5.8 取消退出时强制批量转换，验证应用重启后仍按 60 秒空闲门槛恢复未完成队列

## 6. 桌面与网页照片回放

- [x] 6.1 扩展桌面 `VideoItem` 和分页查询，返回录像 ID、照片状态及网络优先/本地兜底的照片路径
- [x] 6.2 在桌面回放列表异步生成和加载原图等比例缩略图，增加取消、缓存上限、旧录像占位和快速滚动测试
- [x] 6.3 新增只读原图查看器，支持适应窗口、缩放、拖动并显示实际分辨率和拍摄时间
- [x] 6.4 扩展 Web 只读返回模型和照片读取路由，使网页优先显示照片缩略图并可查看原图，同时保持视频首帧缩略图作为旧录像兼容路径
- [x] 6.5 增加路由和界面回归测试，确认网页不存在删除按钮、DELETE API 或将现有访问密钥升级为写权限的行为

## 7. 本机人工删除

- [x] 7.1 在桌面回放每条可删除录像行末增加独立删除按钮，阻止事件冒泡，并根据录像生命周期状态禁用和显示原因
- [x] 7.2 实现包含单号、时间、大小、本地/网络及关联资产范围的二次确认，取消时保证零文件和零数据库变更
- [x] 7.3 实现统一人工删除服务：先写删除意图，再在 `VideoLifecycleCoordinator` 锁内重新核验并取消回放、后处理和归档
- [x] 7.4 幂等删除本地/网络视频、原始照片、联合证明、缩略图缓存及可证明归属的临时文件，并记录逐目标审计结果
- [x] 7.5 网络不可用时显示“等待网络删除”并持久化重试，直到所有已知副本处理完成后才标记人工删除成功
- [x] 7.6 增加当前录像禁止删除、重启重试、重复点击、同名文件、归档与删除竞争及失败任务不得删除他人成果的回归测试

## 8. 设置层级

- [x] 8.1 重构设置窗口左栏为导航区和固定操作区，将“高级模式”开关移动到左侧且不作为 TabItem
- [x] 8.2 保持高级模式配置持久化和所有专业参数即时可见性，并处理关闭时焦点位于隐藏控件的情况
- [x] 8.3 更新设置 XAML 结构测试，验证任意页面都可访问左侧开关且原专业参数分类未被破坏

## 9. 验证、文档与发布准备

- [ ] 9.1 在 N100 8GB 设备上验证正常工作距离的识别率、识别延迟、CPU、内存、录像实际帧率和抓拍到录像首帧耗时
- [ ] 9.2 使用至少一台支持独立静态通道和一台需要分辨率切换的摄像头验证最高原生抓拍、恢复和错误提示
- [ ] 9.3 运行音频探针、强制结束恢复、连续换单、60 秒空闲、10 秒间隔、抢占、网络离线及人工删除端到端测试
- [x] 9.4 更新 README、RELEASE_CHECKLIST 和发布说明，说明每段新录像的原始照片、网页只读、人工删除范围及旧录像无照片兼容行为
- [x] 9.5 运行 `dotnet test ExpressPackingMonitoring.Tests/ExpressPackingMonitoring.Tests.csproj -c Debug`、`dotnet build ExpressPackingMonitoring.sln -c Debug` 和 `Tools/Test-Release-Automated.ps1`，记录未完成的真实设备检查
