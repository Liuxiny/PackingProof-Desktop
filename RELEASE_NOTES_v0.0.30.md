# v0.0.30 录像证据工作流改造

本版本把一次录像扩展为“最终视频 + 原始全分辨率照片 + 联合证明”的完整证据聚合，并针对低功耗 N100 设备减少录像期间的后台 I/O 和重复编码。

## 主要变化

- 面单识别 ROI 跟随实际智能放大范围，关闭放大时识别全画面；候选定位和稳定性判断兼顾位置、尺度、角度、清晰度与曝光
- 每段本机摄像头录像在开录前拍摄稳定、完整视野的最高可用原生分辨率 JPEG，自动识别、扫码枪、网页联动、连续换单及手动录制使用同一启动状态机
- 麦克风 PCM 通过第二输入管道实时编码为 AAC-LC 并写入临时 MKV，正常录像不再先生成 WAV
- 连续空闲 60 秒后才从新到旧封装 MP4，每完成一条暂停 10 秒；新录像会抢占后台封装、校验、哈希、归档和清理任务
- 最终 MP4 与原始照片共同生成版本化签名证明，旧版仅视频证明仍可验证
- 桌面回放增加照片缩略图、原图查看及带二次确认的整段证据删除；网络离线删除可持久化等待并在恢复后续删
- 网页端只读显示照片缩略图和原图，旧录像继续使用视频首帧预览；没有增加删除按钮、DELETE API 或写权限
- “显示高级设置”开关移动到设置左侧固定区域，在任意设置页面均可访问

## 兼容与验证说明

- 旧录像不会被补拍照片，也不会因缺少照片而失去回放能力
- 实际快递面单格式扩展、N100 8GB 性能数据、独立静态通道摄像头和模式切换摄像头仍需按 `RELEASE_CHECKLIST.md` 完成实机验证
